Generate a project charter based on the following inputs.

Project Input:
{input}

Template Context:
{template}

Revision Instructions (if any):
{corrections}

Required Output Schema (Section 11.2):
{{
  "artifact_type": "project_charter_draft",
  "draft_version": 1,
  "content": {{
    "title": "string",
    "sponsor": "string",
    "business_purpose": "string",
    "objectives": ["string"],
    "scope_in": ["string"],
    "scope_out": ["string"],
    "timeline": {{ "start": "string", "end": "string" }},
    "budget": {{ "amount": number, "currency": "string" }},
    "stakeholders": ["string"],
    "risks": []
  }},
  "assumptions": ["string"],
  "missing_information": ["string"],
  "confidence": "low|medium|high",
  "requires_human_confirmation": true
}}

Return ONLY a valid JSON object. Do not include markdown fences.
